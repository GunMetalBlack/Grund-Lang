STRUK Item:
  METH Item(self, pSnacks):
    self.snacks = pSnacks;
  END
END

STRUK student:
  METH student(self, name, absent, period):
    self.Name = name
    self.Absent = absent
    self.Period = period
  END
  METH show_up_to_class(self):
    self.Absent = false;
  END
END

x => student<>; // hate it but stays for now
x.student("bob", true, 6);

GF_WRITE_LN(x.Absent)
x.show_up_to_class()
GF_WRITE_LN(x.Absent)
