// This example shows how to declare and access a list in Grund.

numbers = [1, 2, 3, 4, 5]  // Declare a list named "numbers" and assign some values to it.
GF_WRITE(numbers[0])  // Print the first element of the list.
GF_WRITE(numbers[2])  // Print the third element of the list.
GF_WRITE(numbers[4])  // Print the fifth element of the list.