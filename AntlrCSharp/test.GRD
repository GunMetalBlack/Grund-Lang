jack = 3; 

float = 1.4;

multip = jack * float;

divide = 1 / 2.0;

mod = 3 % 2;

i = jack + float;

Name = "JACK MY MIND IS WORKING";

isworking = true;

GF_WRITE(6 < 7);
GF_WRITE(G_PI,"SHOW PI");
WHILE jack < 7:
    example = 420;
    jack = jack + 1;
    GF_WRITE(jack);
END

GF_WRITE(example);

i = jack;
