jack = 3; 

float = 1.4;

multip = jack * float;

divide = 1 / 2.0;

mod = 3 % 2;

i = jack + float;

Name = "JACK MY MIND IS WORKING";

isworking = true;

GF_WRITE(6 < 7);
GF_WRITE(G_PI,"SHOW PI");

IF 6 < 3:
GF_WRITE("Hello");
END
ELSE:
GF_WRITE("WELP");
END

i = jack;
