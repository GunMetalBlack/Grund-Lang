jack = 3; 

float = 1.4;

multip = jack * float;

divide = 1 / 2.0;

mod = 3 % 2;

i = jack + float;

Name = "JACK MY MIND IS WORKING";

isworking = true;

GF_WRITE(6 < 7);
GF_WRITE(G_PI,"SHOW PI");

// Wow that is the best thing ever!!!!
GF_LIST_CREATE("TOOLS");

GF_LIST_APPEND("TOOLS","PICKAXE");

GF_WRITE(GF_LIST_LOOKUP("TOOLS",0));
IF 6 < 3:
GF_WRITE("Hello");
END
ELSE:
GF_WRITE("WELP");
END

i = jack;
